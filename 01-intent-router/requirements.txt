"""Intent Router service dependencies.
Install with: pip install -r requirements.txt -c ../constraints.txt
Only service-specific (non-core) libs should be versioned here.
"""

fastapi
uvicorn[standard]
pydantic
python-multipart
python-dotenv
httpx

# Optional ML dependencies (left commented to keep base image slim)
# scikit-learn
# numpy
# scipy

# Test dependencies (resolved via constraints for versions)
pytest
pytest-asyncio
